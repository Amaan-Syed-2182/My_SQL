1.select month(order_date) as order_month, avg(total_amount) as avg_order_val from orderswhere year(order_date) = 2025group by Month(order_date);2.SELECT YEAR(order_date) as order_year,MONTHNAME(order_date) AS order_month,SUM(total_revenue) AS total_company_revenue FROM ordersWHERE YEAR(order_date) = 2025GROUP BY MONTH(order_date),Year(order_date)ORDER BY MONTH(order_date);3.select t1.name,t2.order_id,t2.total_amount from customers as t1inner join orders as t2on t1.customer_id = t2.customer_id;4.select t1.name as customer_name, t2.order_id, t2.order_date, t2.total_amount from customers as t1inner join orders as t2on t1.customer_id = t2.customers_idwhere t1.status = 'Active'; 5.select t1.name as customer_name, t2.order_id, t2.order_date, t2.total_amount from customers as t1inner join orders as t2on t1.customer_id = t2.customers_idwhere month(order_date) = 2 and year(order_date) = 2025;6. select customer_name, avg(total_bill) as avg_order_val from customersgroup by customer_id;	